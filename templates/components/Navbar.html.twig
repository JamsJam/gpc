
{#todo flex-r-b-c p7.5    #}

<nav 
    {{ attributes.defaults(
        stimulus_controller('responsivnav')
        |stimulus_action( 'responsivnav','setMobileValue','resize@window')
        ) }}
    class='nav'
    
    >
    {% set currentLocale = app.session.get('_locale') %}
        {% set flags = {
        'en': 'flag-england',
        'fr': 'flag-france',
        'es': 'flag-spain'
    } %}
    {% trans_default_domain 'navigation' %}
        <div class="bandeau-contact">
            <p> Du lundi au samedi de 9h à 17h</p>

            <div><p>contact@guadeloupepassoincaraibes.com</p> <p> 0690.00.00.00</p></div>
        </div>
        {% if loading == false %}


            <div class="logo__container">
                    <img src="{{asset('./images/GPC-logo.png')}}" alt="logo Guadeloupe Passion Caraïbe" class="logo"/>
            </div>
            
            {% if isMobile is not same as true %}
                

                {#todo flex-r-b-c p7.5   #}
                <div class="nav__links ">
                
                    <a href="{{path('app_home')}}"  class="nav__link">{{'nav.desktop.link1'|trans}}</a>
                    <a href="{{path('app_contact')}}"    class="nav__link">{{'nav.desktop.link2'|trans}}</a>
                    <a href="{{path('app_about')}}"  class="nav__link">{{'nav.desktop.link3'|trans}}</a>
                </div>



                    {#todo btn to turbo add modal-form-etape #}
                {# <div class="nav__left"> #}
                    <a href="{{path('app_reservation')}}" class="nav__cta button__nav ">{{'nav.desktop.cta'|trans}}</a>


                    {# {{ ux_icon('mingcute:menu-fill',{'class':'nav__burger'})}}  #}

                {# </div> #}
            {% else %}
                
                <div class="nav__burger" {{stimulus_action('responsivnav','getNavModal','click')}}>
                    {{ ux_icon('mingcute:menu-fill',{'class':'nav__burger'})}} 

                </div>

            {% endif %}
        {% endif %}
</nav>

